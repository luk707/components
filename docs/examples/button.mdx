import { Button } from '../../src'
import { LinkButton, buttonStyled } from '../../src/button'

# Button

<Button>Hello world</Button>

```js
import { Button } from '@luk707/components'

export default () =>
  <Button>Hello world</Button>
```

<Button prominence="primary">Hello world</Button>

```js
import { Button } from '@luk707/components'

export default () =>
  <Button prominence="primary">Hello world</Button>
```

<LinkButton prominence="primary">Hello world</LinkButton>

```js
import { LinkButton } from '@luk707/components/lib/button'

export default () =>
  <LinkButton prominence="primary">Hello world</LinkButton>
```

## Custom component

You may wish to use your own component but have it styled as a button. To do so use the `buttonStyled` funtion and pass it your own component:

```js
import { buttonStyled } from '@luk707/components/lib/button'

const DivButton = buttonStyled('div')

export default () =>
  <DivButton prominence="primary">Hello world</DivButton>
```
